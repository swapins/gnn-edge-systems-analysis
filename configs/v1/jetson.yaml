version: v1

device: cuda
precision: fp32

training:
  epochs: 30
  lr: 0.001
  seed: 42

model:
  hidden_dim: 64

constraints:
  lambda_memory: 0.02
  lambda_time: 0.005

system:
  device_type: jetson
  notes: "Edge deployment on NVIDIA Jetson"